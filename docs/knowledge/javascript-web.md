# JavaScript / Web Notes

Source status: the uploaded `JavaScript_Webb.pdf` is a one-page introductory reference only.

Verified source scope:
- JavaScript can be run in browser developer tools.
- Variables commonly use `let` or `const`.
- Functions may use arrow-function syntax.
- DOM manipulation can change elements on a web page.

Do not treat this note or the uploaded PDF as an advanced authority for browser APIs, event semantics, security, performance, Shadow DOM, MutationObserver behavior, userscript environments, or framework-specific behavior. For those topics, verify against current official documentation and repository evidence.

For Kingshade work, preserve separation between pure decision logic and DOM/browser shell wherever practical, and validate behavior with focused browser tests rather than relying on syntax examples alone.
